console.log("Hello");

var a = 10;
var b = 20;

function add(a, b) {
  var c = a + b;
  return c; // You need to return the result to use it later
}

console.log(add(a, b)); // Call the function with 'a' and 'b' as arguments
